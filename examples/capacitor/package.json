{
  "name": "@point0/example-capacitor",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "point0 dev --hot",
    "generate": "point0 generate",
    "prisma:generate": "bunx prisma generate",
    "prisma:push": "bunx prisma db push",
    "build": "point0 build",
    "start": "cross-env NODE_ENV=production bun run ./dist/server/index.server.js",
    "clean": "rimraf dist",
    "types": "tsc --noEmit",
    "types:6": "bun ../../node_modules/typescript/bin/tsc --noEmit",
    "web": "bun run dev",
    "cap:sync": "bun run build && cap sync",
    "cap:copy": "bun run build && cap copy",
    "cap:add:ios": "cap add ios",
    "cap:add:android": "cap add android",
    "ios": "bun run build && cap sync ios && cap open ios",
    "android": "bun run build && cap sync android && cap open android"
  },
  "dependencies": {
    "@1gr14/error0": "^0.4.8",
    "@1gr14/route0": "^0.3.0",
    "@point0/compiler": "^0.4.0",
    "@point0/core": "^0.4.0",
    "@point0/engine": "^0.4.0",
    "@point0/react-dom": "^0.4.0",
    "@capacitor/core": "^8.5.0",
    "@prisma/adapter-libsql": "^7.9.1",
    "@prisma/client": "^7.9.1",
    "prisma": "^7.9.1",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "server-only": "^0.0.1",
    "superjson": "^2.2.6"
  },
  "devDependencies": {
    "@svgr/core": "^8.1.0",
    "@svgr/plugin-jsx": "^8.1.0",
    "@types/bun": "^1.4.0",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@capacitor/cli": "^8.4.0",
    "@capacitor/android": "^8.4.0",
    "@capacitor/ios": "^8.4.0",
    "cross-env": "^10.0.0",
    "ncp": "^2.0.0",
    "rimraf": "^6.0.1",
    "typescript": "^6.0.3"
  }
}
