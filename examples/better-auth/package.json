{
  "name": "@point0/example-better-auth",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "engines": {
    "bun": ">=1.4.0"
  },
  "scripts": {
    "dev": "point0 dev --hot",
    "generate": "point0 generate",
    "build": "point0 build",
    "start": "cross-env NODE_ENV=production bun run ./dist/server/index.server.js",
    "docker:build": "docker compose build",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down",
    "docker:down:volumes": "docker compose down --volumes",
    "clean": "rimraf dist",
    "types": "tsc --noEmit",
    "types:6": "bun ../../node_modules/typescript/bin/tsc --noEmit",
    "prisma:reset": "cross-env NODE_ENV=development prisma migrate reset",
    "prisma:generate": "cross-env NODE_ENV=development prisma generate",
    "prisma:migrate:dev": "cross-env NODE_ENV=development prisma migrate dev",
    "prisma:migrate:deploy": "cross-env NODE_ENV=development prisma migrate deploy",
    "seed": "cross-env NODE_ENV=development bun --preload ./src/preload.ts ./src/lib/seed.ts",
    "setup": "bun run prisma:migrate:deploy && bun run prisma:generate && point0 generate && bun run seed"
  },
  "dependencies": {
    "@1gr14/error0": "^0.4.8",
    "@1gr14/flat": "^0.1.4",
    "@1gr14/route0": "^0.3.0",
    "@point0/basic-auth": "^0.4.0",
    "@point0/cache-control": "^0.4.0",
    "@point0/compiler": "^0.4.0",
    "@point0/compress": "^0.4.0",
    "@point0/core": "^0.4.0",
    "@point0/engine": "^0.4.0",
    "@point0/openapi": "^0.4.0",
    "@point0/react-dom": "^0.4.0",
    "@prisma/adapter-libsql": "^7.9.1",
    "@prisma/client": "^7.9.1",
    "@tanstack/react-query": "^5.101.4",
    "@unhead/react": "^3.2.3",
    "better-auth": "^1.6.25",
    "bun-plugin-tailwind": "^0.1.2",
    "dotenv": "^17.4.2",
    "openapi-types": "^12.0.0",
    "prisma": "^7.9.1",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "superjson": "^2.2.6",
    "tailwindcss": "^4.3.3",
    "unhead": "^3.2.3",
    "wouter": "^3.10.0"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.20",
    "@svgr/core": "^8.1.0",
    "@svgr/plugin-jsx": "^8.1.0",
    "@tanstack/react-query-devtools": "^5.101.4",
    "@types/bun": "^1.4.0",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@faker-js/faker": "^10.5.0",
    "cross-env": "^10.0.0",
    "ncp": "^2.0.0",
    "rimraf": "^6.0.1",
    "typescript": "^6.0.3"
  }
}
